const fs = require('fs'); const modalPath = 'src/components/work-orders/NewWorkOrderModal.jsx'; if (fs.existsSync(modalPath)) { let content = fs.readFileSync(modalPath, 'utf8'); if (!content.includes('export default')) { content += '\nexport default NewWorkOrderModal;\n'; fs.writeFileSync(modalPath, content, 'utf8'); console.log('OK: Export default agregado en NewWorkOrderModal.jsx'); } else { console.log('OK: Ya tenia export default'); } }
